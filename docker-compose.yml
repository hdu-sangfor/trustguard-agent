name: trustguard-agent

x-python-env: &python-env
  WORKSPACE_ROOT: /data/workspace
  REDIS_URL: redis://redis:6379/0
  MQ_BROKER_URL: amqp://guest:guest@rabbitmq:5672/
  MQ_TOPIC_AGENT: execute_tasks_agent

x-optional-env-files: &optional-env-files
  - path: .env
    required: false

x-build-args: &build-args
  DOCKERHUB_REGISTRY: ${DOCKERHUB_REGISTRY:-docker.io}
  TRUSTGUARD_NETWORK_PROXY: ${TRUSTGUARD_NETWORK_PROXY:-}
  NPM_CONFIG_REGISTRY: ${NPM_CONFIG_REGISTRY:-}
  UV_INDEX_URL: ${UV_INDEX_URL:-}
  UV_EXTRA_INDEX_URL: ${UV_EXTRA_INDEX_URL:-}
  UV_DEFAULT_INDEX: ${UV_DEFAULT_INDEX:-}
  PIP_INDEX_URL: ${PIP_INDEX_URL:-}
  PIP_EXTRA_INDEX_URL: ${PIP_EXTRA_INDEX_URL:-}
  DEBIAN_MIRROR_URL: ${DEBIAN_MIRROR_URL:-}
  DEBIAN_SECURITY_MIRROR_URL: ${DEBIAN_SECURITY_MIRROR_URL:-}
  GOPROXY: ${GOPROXY:-}
  ALPINE_MIRROR_URL: ${ALPINE_MIRROR_URL:-}
  RUBYGEMS_MIRROR_URL: ${RUBYGEMS_MIRROR_URL:-}

x-build-extra-hosts: &build-extra-hosts
  - "host.docker.internal:host-gateway"

x-build-network: &build-network host

services:
  mysql:
    image: ${MYSQL_IMAGE:-${DOCKERHUB_REGISTRY:-docker.io}/library/mysql:8.0}
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-trustguard_agent}
      MYSQL_USER: ${MYSQL_USER:-trustguard}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-trustguard}
    volumes:
      - mysql_data:/var/lib/mysql
      - ./docker/mysql-init.d:/docker-entrypoint-initdb.d:ro
      - ./docker/conf.d:/etc/mysql/conf.d:ro
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "root", "-p${MYSQL_ROOT_PASSWORD:-root}"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: ${REDIS_IMAGE:-${DOCKERHUB_REGISTRY:-docker.io}/library/redis:7-alpine}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  rabbitmq:
    image: ${RABBITMQ_IMAGE:-${DOCKERHUB_REGISTRY:-docker.io}/library/rabbitmq:3-management-alpine}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 25s

  qdrant:
    image: ${QDRANT_IMAGE:-${DOCKERHUB_REGISTRY:-docker.io}/qdrant/qdrant:latest}
    volumes:
      - qdrant_data:/qdrant/storage
    restart: unless-stopped

  evidence:
    build:
      context: ./evidence
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_IMAGE_PREFIX:-trustguard-}evidence
    restart: unless-stopped
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: ${MYSQL_USER:-trustguard}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-trustguard}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-trustguard_agent}
      WORKSPACE_ROOT: /data/workspace
      SERVER_PORT: 18103
    volumes:
      - workspace:/data/workspace
    depends_on:
      mysql: { condition: service_healthy }
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:18103/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  supervisor:
    build:
      context: ./supervisor
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_IMAGE_PREFIX:-trustguard-}supervisor
    restart: unless-stopped
    environment:
      REDIS_URL: redis://redis:6379/0
      SUPERVISOR_STORE_BACKEND: redis
      SUPERVISOR_CONVERSATION_STORE_BACKEND: mysql+redis
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: ${MYSQL_USER:-trustguard}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-trustguard}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-trustguard_agent}
      DEPLOYMENT_MODE: docker
      SERVER_PORT: 18082
      SUPERVISOR_CONFIRMATION_SECRET: ${SUPERVISOR_CONFIRMATION_SECRET:-trustguard-supervisor-dev-secret}
    env_file: *optional-env-files
    depends_on:
      redis: { condition: service_healthy }
      mysql: { condition: service_healthy }
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:18082/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  orchestrator:
    build:
      context: ./orchestrator
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_IMAGE_PREFIX:-trustguard-}orchestrator
    restart: unless-stopped
    environment:
      <<: *python-env
      EVIDENCE_BASE_URL: http://evidence:18103
      EXECUTOR_BASE_URL: http://executor:18102
      ENABLE_EXECUTOR: "true"
      KB_QDRANT_URL: http://qdrant:6333
      SKILLS_ROOT: /app/skills
      TOOLS_REGISTRY_YAML: /app/tools_registry.yaml
      ORCH_TASK_STORE_BACKEND: redis
      ORCH_DEFAULT_RUN_MAX_DURATION_SECONDS: ${ORCH_DEFAULT_RUN_MAX_DURATION_SECONDS:-900}
      ORCH_FP_T1_ENABLED: ${ORCH_FP_T1_ENABLED:-false}
      ORCH_FP_T2_MAX_TOKENS_PER_TASK: ${ORCH_FP_T2_MAX_TOKENS_PER_TASK:-10000}
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: ${MYSQL_USER:-trustguard}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-trustguard}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-trustguard_agent}
      ORCH_MAX_PLAN_ROUNDS_PER_PHASE: "10"
      ORCH_PHASE_WALL_CLOCK_LIMIT_SEC: "300"
      TRUSTGUARD_AGENT_MODE: deterministic
      TRUSTGUARD_EXECUTOR_MODE: inline
      LEGACY_STATIC_KB_READ_ENABLED: ${LEGACY_STATIC_KB_READ_ENABLED:-true}
      LEGACY_EXPERIENCE_READ_ENABLED: ${LEGACY_EXPERIENCE_READ_ENABLED:-true}
      KNOWLEDGE_MCP_ENABLED: ${KNOWLEDGE_MCP_ENABLED:-false}
      KNOWLEDGE_MCP_SHADOW_MODE: ${KNOWLEDGE_MCP_SHADOW_MODE:-true}
      KNOWLEDGE_MCP_MATERIALIZE_ENABLED: ${KNOWLEDGE_MCP_MATERIALIZE_ENABLED:-false}
      KNOWLEDGE_MCP_INJECT_ENABLED: ${KNOWLEDGE_MCP_INJECT_ENABLED:-false}
      KNOWLEDGE_MCP_MATERIALIZE_LIMIT: ${KNOWLEDGE_MCP_MATERIALIZE_LIMIT:-3}
      KNOWLEDGE_MCP_URL: ${KNOWLEDGE_MCP_URL:-http://host.docker.internal:18201/mcp}
      KNOWLEDGE_MCP_ACCESS_TOKEN: ${KNOWLEDGE_MCP_ACCESS_TOKEN:-}
      KNOWLEDGE_MCP_PENETRATION_SCOPE: ${KNOWLEDGE_MCP_PENETRATION_SCOPE:-penetration}
      KNOWLEDGE_MCP_ALERT_TRIAGE_SCOPE: ${KNOWLEDGE_MCP_ALERT_TRIAGE_SCOPE:-alert-triage}
      KNOWLEDGE_MCP_MODE: ${KNOWLEDGE_MCP_MODE:-comprehensive}
      KNOWLEDGE_MCP_LIMIT: ${KNOWLEDGE_MCP_LIMIT:-5}
      KNOWLEDGE_MCP_REWRITE: ${KNOWLEDGE_MCP_REWRITE:-false}
      KNOWLEDGE_MCP_TIMEOUT_SECONDS: ${KNOWLEDGE_MCP_TIMEOUT_SECONDS:-8}
      KNOWLEDGE_MCP_MAX_RETRIES: ${KNOWLEDGE_MCP_MAX_RETRIES:-1}
      KNOWLEDGE_MCP_WORKSPACE_ID: ${KNOWLEDGE_MCP_WORKSPACE_ID:-default}
      KNOWLEDGE_MCP_WORKFLOW_TYPE: ${KNOWLEDGE_MCP_WORKFLOW_TYPE:-penetration}
      XDR_BASE_URL: http://host.docker.internal:8443
      XDR_AK: test_ak_0001
      XDR_SK: test_sk_0001_secret
      LLM_PROVIDER: ${LLM_PROVIDER:-openai_compat}
      LLM_BASE_URL: ${LLM_BASE_URL:-http://localhost:11434/v1}
      LLM_API_KEY: ${LLM_API_KEY:-}
      LLM_MODEL_ID: ${LLM_MODEL_ID:-gpt-4o-mini}
    env_file: *optional-env-files
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./skills:/app/skills:ro
      - ./docker/tools_registry.yaml:/app/tools_registry.yaml:ro
      - workspace:/data/workspace
    depends_on:
      mysql: { condition: service_healthy }
      evidence: { condition: service_healthy }
      executor: { condition: service_started }
      redis: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      qdrant: { condition: service_started }
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:18081/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  executor:
    build:
      context: ./executor
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_IMAGE_PREFIX:-trustguard-}executor
    restart: unless-stopped
    working_dir: /srv/executor
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "18102"]
    environment:
      <<: *python-env
      TRUSTGUARD_TOOLS_ROOT: /app/TRUSTGUARD_TOOLS_ROOT
      TOOLS_REGISTRY_YAML: /app/tools_registry.yaml
      TRUSTGUARD_SKILLS_ROOT: /app/skills
      EXECUTOR_LOCALHOST_ALIAS: host.docker.internal
      EXECUTOR_POINTER_ONLY: "true"
      EXECUTOR_USE_SKILL_CONTAINERS: "true"
      EXECUTOR_SKILL_IMAGE_PREFIX: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}
      EXECUTOR_DOCKER_NETWORK: trustguard-agent_default
      EXECUTOR_SCRIPT_DEFAULT_TIMEOUT_SECONDS: "90"
      EXECUTOR_SCRIPT_HARD_TIMEOUT_SECONDS: "120"
      EXECUTOR_SCRIPT_NUCLEI_DEFAULT_TIMEOUT_SECONDS: "300"
      EXECUTOR_SCRIPT_NUCLEI_HARD_TIMEOUT_SECONDS: "600"
      NMAP_HARD_TIMEOUT_SECONDS: "120"
    env_file: *optional-env-files
    volumes:
      - executor_tools:/app/TRUSTGUARD_TOOLS_ROOT
      - ./skills:/app/skills:ro
      - ./docker/tools_registry.yaml:/app/tools_registry.yaml:ro
      - workspace:/data/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    extra_hosts:
      - "host.docker.internal:host-gateway"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:18102/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  mq-agent-worker:
    build:
      context: ./executor
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_IMAGE_PREFIX:-trustguard-}executor
    working_dir: /srv/executor
    command: ["python", "-m", "app.mq_agent_daemon"]
    environment:
      <<: *python-env
      TRUSTGUARD_TOOLS_ROOT: /app/TRUSTGUARD_TOOLS_ROOT
      TOOLS_REGISTRY_YAML: /app/tools_registry.yaml
      TRUSTGUARD_SKILLS_ROOT: /app/skills
      EXECUTOR_LOCALHOST_ALIAS: host.docker.internal
      EXECUTOR_POINTER_ONLY: "true"
      EXECUTOR_USE_SKILL_CONTAINERS: "true"
      EXECUTOR_SKILL_IMAGE_PREFIX: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}
      EXECUTOR_DOCKER_NETWORK: trustguard-agent_default
    env_file: *optional-env-files
    volumes:
      - executor_tools:/app/TRUSTGUARD_TOOLS_ROOT
      - ./skills:/app/skills:ro
      - ./docker/tools_registry.yaml:/app/tools_registry.yaml:ro
      - workspace:/data/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      redis: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
    restart: unless-stopped

  gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_IMAGE_PREFIX:-trustguard-}gateway
    restart: unless-stopped
    ports:
      - "${TRUSTGUARD_BIND_HOST:-127.0.0.1}:18080:18080"
    environment:
      MYSQL_HOST: mysql
      MYSQL_PORT: 3306
      MYSQL_USER: ${MYSQL_USER:-trustguard}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD:-trustguard}
      MYSQL_DATABASE: ${MYSQL_DATABASE:-trustguard_agent}
      REDIS_URL: redis://redis:6379/0
      MQ_BROKER_URL: amqp://guest:guest@rabbitmq:5672/
      ORCHESTRATOR_BASE_URL: http://orchestrator:18081
      EVIDENCE_BASE_URL: http://evidence:18103
      EXECUTOR_BASE_URL: http://executor:18102
      SUPERVISOR_BASE_URL: http://supervisor:18082
      RAG_SERVICE_BASE_URL: ${RAG_SERVICE_BASE_URL:-http://host.docker.internal:18200}
      RAG_GATEWAY_SERVICE_TOKEN: ${RAG_GATEWAY_SERVICE_TOKEN:-}
      AUTH_TOKEN_SECRET: ${AUTH_TOKEN_SECRET:-trustguard-agent-dev-secret}
      AUTH_TOKEN_TTL_SECONDS: ${AUTH_TOKEN_TTL_SECONDS:-86400}
      RAG_UPLOAD_MAX_BYTES: ${RAG_UPLOAD_MAX_BYTES:-52428800}
      SERVER_PORT: 18080
    env_file: *optional-env-files
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      mysql: { condition: service_healthy }
      redis: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      evidence: { condition: service_healthy }
      supervisor: { condition: service_healthy }
      orchestrator: { condition: service_healthy }
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:18080/health')"]
      interval: 10s
      timeout: 5s
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_IMAGE_PREFIX:-trustguard-}frontend
    ports:
      - "${TRUSTGUARD_BIND_HOST:-127.0.0.1}:17999:80"
    depends_on:
      gateway: { condition: service_healthy }
    restart: unless-stopped

  skill-curl-raw:
    profiles: ["skills"]
    build:
      context: ./skills/curl-raw
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}curl-raw:latest

  skill-nmap:
    profiles: ["skills"]
    build:
      context: ./skills/nmap
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}nmap:latest

  skill-http-enum:
    profiles: ["skills"]
    build:
      context: ./skills/http-enum
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}http-enum:latest

  skill-httpx:
    profiles: ["skills"]
    build:
      context: ./skills/httpx
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}httpx:latest

  skill-ehole:
    profiles: ["skills"]
    build:
      context: ./skills/ehole
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}ehole:latest

  skill-dirsearch:
    profiles: ["skills"]
    build:
      context: ./skills/dirsearch
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}dirsearch:latest

  skill-ffuf-dir-enum:
    profiles: ["skills"]
    build:
      context: ./skills/ffuf-dir-enum
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}ffuf-dir-enum:latest

  skill-katana:
    profiles: ["skills"]
    build:
      context: ./skills
      dockerfile: katana/Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}katana:latest

  skill-dispatcher:
    profiles: ["skills"]
    build:
      context: ./skills
      dockerfile: dispatcher/Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}dispatcher:latest

  skill-nuclei:
    profiles: ["skills"]
    build:
      context: ./skills
      dockerfile: nuclei/Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}nuclei:latest

  skill-nikto-scan:
    profiles: ["skills"]
    build:
      context: ./skills/nikto-scan
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}nikto-scan:latest

  skill-fscan:
    profiles: ["skills"]
    build:
      context: ./skills/fscan
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}fscan:latest

  skill-metasploit:
    profiles: ["skills"]
    build:
      context: ./skills/metasploit
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}metasploit:latest

  skill-metasploit-session:
    profiles: ["skills"]
    build:
      context: ./skills/metasploit-session
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}metasploit-session:latest

  skill-python-sandbox:
    profiles: ["skills"]
    build:
      context: ./skills/python-sandbox
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}python-sandbox:latest

  skill-sqlmap:
    profiles: ["skills"]
    build:
      context: ./skills/sqlmap
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}sqlmap:latest

  skill-fenjing:
    profiles: ["skills"]
    build:
      context: ./skills/fenjing
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}fenjing:latest

  skill-baidu-search:
    profiles: ["skills"]
    build:
      context: ./skills/baidu-search
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}baidu-search:latest
    env_file:
      - path: .env
        required: false
    environment:
      BAIDU_API_KEY: ${BAIDU_API_KEY:-}

  skill-read-workspace-artifact:
    profiles: ["skills"]
    build:
      context: ./skills/read_workspace_artifact
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}read_workspace_artifact:latest

  skill-read-target-list:
    profiles: ["skills"]
    build:
      context: ./skills/read_target_list
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}read_target_list:latest

  skill-webshell-php:
    profiles: ["skills"]
    build:
      context: ./skills/webshell-php
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}webshell-php:latest

  skill-whatweb-fingerprint:
    profiles: ["skills"]
    build:
      context: ./skills/whatweb-fingerprint
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}whatweb-fingerprint:latest

  skill-linpeas:
    profiles: ["skills"]
    build:
      context: ./skills/linpeas
      dockerfile: Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}linpeas:latest

  skill-v1-micro-sample:
    profiles: ["skills"]
    build:
      context: .
      dockerfile: skills/v1-micro-sample/Dockerfile
      args: *build-args
      extra_hosts: *build-extra-hosts
      network: *build-network
    image: ${TRUSTGUARD_SKILL_IMAGE_PREFIX:-trustguard-skill-}v1-micro-sample:latest

volumes:
  mysql_data:
  qdrant_data:
  workspace:
  executor_tools:
