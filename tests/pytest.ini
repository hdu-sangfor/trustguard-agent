[pytest]
# 本地 pytest 配置：仅收集本仓库 tests 目录。
testpaths = tests
# 本地可用 `pytest -c tests/pytest.ini -m "not integration and not smoke"` 跳过外部依赖用例。
markers =
    integration: 需外部服务的集成测试
    smoke: 需本机编排器/Executor 可达
    e2e: 靶场式 E2E；需 E2E_TARGET_URL 等（见 tests/conftest.py）
