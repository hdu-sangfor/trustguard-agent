name: Python Executor

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: python-executor-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.11"
  UV_VERSION: "0.11.7"
  EXECUTOR_USE_SKILL_CONTAINERS: "true"
  TRUSTGUARD_EXECUTOR_MODE: inline

jobs:
  executor:
    name: Python Executor
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: true
          cache-dependency-glob: |
            executor/pyproject.toml
            executor/uv.lock

      - name: Install executor dependencies
        working-directory: executor
        run: uv sync --locked --no-dev --no-install-project --python "$(which python)"

      - name: Check installed dependencies
        working-directory: executor
        run: uv pip check

      - name: Compile executor modules
        working-directory: executor
        run: uv run --locked --no-dev --no-sync python -m compileall app ../dev/mq

      - name: Import executor entrypoint
        working-directory: executor
        env:
          PYTHONPATH: .
        run: uv run --locked --no-dev --no-sync python -c "import app.main"
